import ReactDOM from "react-dom/client"; // Import ReactDOM from the react-dom/client package

import App from "./App"; // Import the App component
import './index.css'; // Import the main stylesheet


ReactDOM.createRoot(document.getElementById('root')).render(<App />); // Render the App component inside the root div
